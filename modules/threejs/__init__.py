"""Descriptor building for the Three.js nodes.

Nothing here touches a renderer. Each module turns node inputs into plain dicts, and the
browser resolves those into Three.js objects. Nothing is re-exported at package level.
"""
