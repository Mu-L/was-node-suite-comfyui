"""What the ported nodes share with each other rather than with any one of them.

:mod:`.types` holds the pack's custom socket types; :mod:`.replacements` holds the v2 ->
v3 ``io.NodeReplace`` table and registers it.
"""
