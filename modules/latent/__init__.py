"""Latent-space maths shared by more than one latent node.

:mod:`.shape` moves a latent between the 4D image layout and the 5D video layout.
:mod:`.filters` holds the spatial filters the detail and upscale nodes are built from.
"""
