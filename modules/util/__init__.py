"""Small utilities that belong to no single node area.

``sandbox`` contains user-supplied paths; ``hashing`` derives seeds and digests;
``easing`` holds interpolation curves; ``numbers`` reads a widget value as a number;
``text_files`` lists text files; ``net`` downloads over HTTP.
"""
