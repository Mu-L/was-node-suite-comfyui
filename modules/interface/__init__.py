"""What the node interfaces in ``web/`` need from the server.

:mod:`.preview` and :mod:`.pixels` serve a node's pictures, :mod:`.run_result` and the
report modules what a run did, :mod:`.lines`, :mod:`.fonts` and :mod:`.pantry` what a picker
draws. :mod:`.channel` holds what each needs.
"""
