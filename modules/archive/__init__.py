"""What the archive nodes need for the zip files they read and write.

:mod:`.container` opens an archive, :mod:`.kinds` names the files the pack can load,
:mod:`.save` writes one from disk, and :mod:`.summary` reads one out for a person.
"""
